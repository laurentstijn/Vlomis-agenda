name: "30-Minute Sync Heartbeat"

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel Endpoint
        run: |
          curl -s https://vlomis-agenda.vercel.app/api/vlomis
